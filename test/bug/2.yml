default: test

properties:
  FOO: 'foo'

environment:
  FOO: '#{uppercase(FOO)}'

targets:

  test:
    steps:
    - $: 'echo $FOO'
      =: 'result'
    - if: 'result != "FOO"'
      then:
      - throw: 'Environment evaluation failed'
