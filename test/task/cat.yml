doc: Built file to test tasks
default: task_cat

properties:
  BUILD_DIR: "../../build"

targets:

  task_cat:
    doc: Test task cat
    steps:
    - mkdir: "#{BUILD_DIR}/cat"
    - touch: "#{BUILD_DIR}/cat/test.txt"
    - cat: "#{BUILD_DIR}/cat/test.txt"
